package abc.to50.abc049

fun main() {
    val (H, W) = readLine()!!.split(" ").map(String::toInt)
    val pics = List(H) { readLine()!! }
    pics.forEach {
        println(it)
        println(it)
    }
}